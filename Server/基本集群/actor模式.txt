actor模式是一种并发模型上。
与actor模式相对的是共享内存模型，共享内存模型即为大家共享一片内存，大家都有权访问（增删改查）该内存片。

actor模型不共享任何东西，每个对象（进程，线程，甚至最近比较流行的协程，纤程）唯一标识一个actor，处理方式即为向该actor发送，接收信息。

Erlang，Golang这两种语言的设计就是一种典型的actor模式语言。

所有的处理都是基于往该对象发送消息，然后返回给发送者相应回应（一般就是接收是否成功，处理是否成功）。

这部分如果结合名字服务，将会大大的简化很多异步操作。



