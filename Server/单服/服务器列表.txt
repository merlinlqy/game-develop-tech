游戏基本都会分区分服，那客户端如何知道连接到哪个服务器呢。这里一般都会采用服务器列表的方式。

服务器列表一般会涉及3-5个域名解释的服务器，客户端在打开以后，将会往这些服务器发送请求，获取服务器列表。

该列表即可知道整个所有在线服务器。

列表服务器因为已知他的域名，因此要采取很多防止攻击的手段。例如DDOS，域名动态映射IP，区域分割等。

而列表服务器返回的列表一般也不做加密。
加密都会放在后一步，连接服务器之中。


